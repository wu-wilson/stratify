import styles from "./Invite.module.scss";

const Invite = () => {
  return (
    <div className={styles.container}>
      <div className={styles.header}>Invite New Members</div>
    </div>
  );
};

export default Invite;
